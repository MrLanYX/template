// 拉取依赖
import Vue from 'vue';
import VueRouter from 'vue-router';
Vue.use(VueRouter);
// 引入所需文件
// import Home from '@/pages/home.vue';

export default new VueRouter({
    routes: [
        // {
        // path: '/',
        // name: 'home',
        // component: Home,
        // redirect: '/index',
        // children: []
        // }
    ]
})